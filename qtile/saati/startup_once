#!/usr/bin/env bash
folder_of_script=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)
xfsettingsd --daemon -D & disown
eval "$(which lxqt-policykit-agent)" & disown
dunst -conf "${folder_of_script}"/dunstrc & disown
# xfce4-panel -d & disown
nm-applet & disown
# eval "$folder_of_script/qtile/saati/keyring.sh" & disown
oblivion & disown
nm-tray & disown
nitrogen --restore & disown
dropbox start & disown
# proxychains4-daemon & disown
firefox & disown
# gtk-launch discord & disown
/home/sdsaati/Downloads/Logseq-linux-x64-0.10.9.AppImage --no-sandbox & disown
# gtk-launch Obsidian.desktop & disown
blueman-applet & disown
